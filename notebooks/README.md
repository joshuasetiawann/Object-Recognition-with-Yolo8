# Notebooks

Use this directory for Jupyter notebooks — quick experiments, dataset
exploration, or visualizing detection results.

Keep reusable logic in `src/` and import it from your notebooks rather than
duplicating code.
